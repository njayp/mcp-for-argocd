{
    "servers": {
        "argocd-mcp-server": {
            "type": "stdio",
            "command": "node",
            "args": [
                "${workspaceFolder}/dist/argocd-mcp-server.js",
                "stdio"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}